<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/assets/css/bootstrap.min2.css">
    <script src="/assets/js/bootstrap.min2.js" type="text/javascript"></script>
    <title>ĐĂNG NHẬP</title>
</head>
<body>
  <% if (messages.error && messages.error.length > 0) { %>
    <div class="container-fluid">
    <main class="tm-main">
    <div class="alert alert-danger">
      <%= messages.error[0] %>
    </div>
    </main>
    </div>
  <%}%>
    <div class="back">
      <div class="div-center">
        <div class="content">
          <h3>ĐĂNG NHẬP</h3>
          <hr />
          <form action="/login" method="post">
            <div class="form-group">
              <label for="exampleInputEmail1">Gmail</label>
              <input type="text" class="form-control" id="exampleInputEmail1" placeholder="Tên Đăng Nhập" name="username">
              <% if(check==0){ %>
                <p style="color:red;" class="text-start"> Gmail không đúng. </p>
              <% } %>
            </div>
            <div class="form-group">
              <label for="exampleInputPassword1">Mật Khẩu</label>
              <input type="password" class="form-control" id="exampleInputPassword1" placeholder="Mật Khẩu" name="password">
              <% if(check==0){ %>
                <p style="color:red;" class="text-start"> Mật khẩu không đúng. </p>
              <% } %>
            </div>
            <button type="submit" class="btn btn-primary">Đăng Nhập</button>
            <hr/>
            <button type="button" class="btn btn-link"><a href="/dangki">Đăng Kí</a></button>
            <button type="button" class="btn btn-link">Reset Password</button>
          </form>
        </div>
        </span>
      </div>
  </body>
</html>